import resolveDate from './modules/time.js';
import {
  addRemoveEvents,
  addSubmitEvent,
  addClickEvents,
} from './modules/events.js';

resolveDate();
addRemoveEvents();
addSubmitEvent();
addClickEvents();
